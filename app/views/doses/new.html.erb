<%= render "./cocktails/navbar"%>

<div class="container">

  <h1>Add a new dose</h1>
  <br>
  <%= simple_form_for [ @cocktail, @dose ] do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.submit "Submit dose", class: "btn-flat" %>
  <% end %>
</div>
